# 足球带球移动控制 — 实现文档

## Context

足球在地面的带球移动控制，仅 XZ 平面运动（Y 恒为 0）。角色在编辑器中自由移动，代码以 120ms 固定间隔向足球施加经过摩擦力衰减的瞬时速度，模拟"追球→踢球→减速→再追球"循环。

## 修改文件

**唯一修改：`src/main.ts`**

## 架构：编辑器定时器 + 事件分离

使用编辑器定时器替代 `setInterval`，注册与处理分离：

```ts
g.server({ id, lang: 'zh', variables }).on('实体创建时', (_evt, f) => {
  f.启动定时器(self, 'run', true, [0.12])
}).on('定时器触发时', (_evt, f) => {
  // 每 120ms 执行的带球逻辑
})
```

`f.启动定时器` 第四参数为 `[间隔秒数]` 数组。

## 节点图变量

```typescript
variables: {
  _init: false,       // 一次性初始化哨兵
  ballVx: 0.0,        // 足球 X 方向速度
  ballVz: 0.0,        // 足球 Z 方向速度（Y 恒为 0，不存）
  tickCount: 0n,      // tick 计数器 (bigint)
  lastKickTick: 0n    // 上次踢球的 tick 序号
}
```

## 核心算法流程 (每个 120ms tick)

```
1. tickCount++
2. 查找足球 (prefabId 1077936262) 和 角色 (self)
3. 读取角色位置、前方向量、速度
   - 前向 = 三维向量旋转(欧拉角, [0,0,1.0])  ← 不用 getEntityForwardVector
4. 读取足球位置
5. 存储球速 *= 0.95 (摩擦力衰减，仅 XZ)
6. 踢球决策:
   - 条件: tickSinceKick>=3 && distSq<2.25 && forwardDotBall>-0.3
   - 方向: normalize(0.7*charForward + 0.3*toBallDir)
   - 基础力: max(charSpeed*1.2, 3.0)
   - 对齐权重: 分段 linear (1.2/1.0/0.5/0.2/0.1)
   - 力系数: 轻推0.3 / 修正1.2 / 正常0.8
   - 最终速度 = 基础力 * 权重 * 系数
7. 叠加踢球速度：newVel = 三维向量加法(prevVel, 三维向量缩放(kickDir, kickSpeed))
8. 写回 ballVx/ballVz
9. 添加匀速直线型基础运动器(ball, 'dribbleCtrl', 0.12, finalVel)
10. 设置自定义变量(ball, '速度', finalVel)  ← 同步速度用于调试/其他逻辑
```

## 关键编程规范

- **所有节点函数通过 `f.*` 调用**，中文别名来自 `node_modules/genshin-ts/src/definitions/zh_aliases.ts`
- **创建三维向量用 `f.创建三维向量(x, y, z)`**，不能 `vec3([x,y,z])`（后者只接受字面量）
- **角色朝向**：`f.三维向量旋转(charLocRot.rotate, f.创建三维向量(0.0, 0.0, 1.0))`
- **全部局部变量用 `let`**，if-else 赋值模式，非多 return
- **复合布尔条件用 `bool(...)` 包裹**
- **`_init` 哨兵**确保启动逻辑只执行一次

## 编辑器前置条件

1. 地图中放置足球实体（prefabId **1077936262**）
2. 目标节点图 ID **1073742432** 需在地图中存在（空图即可，名称以 `_GSTS` 开头）
3. `gsts.config.ts` 的 `mapId` 需匹配目标地图
4. 节点图挂载在角色实体上（`self` = 角色）
5. 需要注册编辑器定时器 `'run'` 和事件 `'定时器触发时'`

## WSL 环境配置

```bash
export LOCALAPPDATA=/mnt/c/Users/touyu/AppData/Local
# 已写入 ~/.bashrc
```

## 验证流程

```bash
npm run typecheck  # 先过类型
npm run lint       # 再过 ESLint
npm run build      # 编译 + GIA + 注入
# 检查 dist/src/main.gs.ts 确认代码正确展开
```
